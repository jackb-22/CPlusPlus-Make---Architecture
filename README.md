# C-Make---Architecture
A C++17 `make`-style build system that parses `MyMakefile`, resolves dependency graphs, applies implicit C/C++ build rules, caches parsed rules, and executes only stale targets. 
